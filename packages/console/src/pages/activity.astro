---
import LayoutSidebar from '../app/LayoutSidebar.astro';

// Sample activity data - replace with actual data source
const activities = [
	{
		id: 'TXN-001',
		type: 'Investment',
		description: 'Investment to loan MBDF-24100493',
		amount: 200.00,
		status: 'SUCCESS',
		date: '2024-10-23',
		time: '12:05:29'
	},
	{
		id: 'TXN-002',
		type: 'Investment',
		description: 'Investment to loan MBDF-24100678',
		amount: 300.00,
		status: 'SUCCESS',
		date: '2024-10-23',
		time: '12:03:56'
	},
	{
		id: 'TXN-003',
		type: 'Investment',
		description: 'Investment to loan MBDF-24100289',
		amount: 200.00,
		status: 'SUCCESS',
		date: '2024-10-23',
		time: '12:02:33'
	},
	{
		id: 'TXN-004',
		type: 'Deposit',
		description: 'Account top-up via bank transfer',
		amount: 1000.00,
		status: 'SUCCESS',
		date: '2024-10-22',
		time: '09:15:42'
	},
	{
		id: 'TXN-005',
		type: 'Withdrawal',
		description: 'Withdrawal to bank account',
		amount: 150.00,
		status: 'PENDING',
		date: '2024-10-21',
		time: '14:30:18'
	}
];

const formatCurrency = (amount: number) => {
	return `RM ${amount.toLocaleString('en-MY', {
		minimumFractionDigits: 2,
		maximumFractionDigits: 2
	})}`;
};

const formatDateTime = (date: string, time: string) => {
	return `${new Date(date).toLocaleDateString('en-GB', {
		day: '2-digit',
		month: 'short',
		year: 'numeric'
	})} ${time}`;
};

const getStatusColor = (status: string) => {
	switch (status) {
		case 'SUCCESS':
			return 'bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-300';
		case 'PENDING':
			return 'bg-yellow-100 text-yellow-800 dark:bg-yellow-900 dark:text-yellow-300';
		case 'FAILED':
			return 'bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-300';
		default:
			return 'bg-gray-100 text-gray-800 dark:bg-gray-900 dark:text-gray-300';
	}
};
---

<LayoutSidebar>
	<div class="p-4 bg-white border-b border-gray-200 lg:mt-1.5 dark:bg-gray-800 dark:border-gray-700">
		<div class="mb-4">
			<h1 class="text-xl font-semibold text-gray-900 sm:text-2xl dark:text-white">
				My Activity
			</h1>
			<p class="text-sm text-gray-500 dark:text-gray-400 mt-1">
				Track all your transactions and activities
			</p>
		</div>
	</div>

	<div class="px-4 pt-6">
		<!-- Activity Table -->
		<div class="mb-4">
			<div
				class="p-4 bg-white border border-gray-200 rounded-lg shadow-sm sm:p-6 dark:border-gray-700 dark:bg-gray-800"
			>
				<div class="flex items-center justify-between mb-4">
					<h3 class="text-lg font-semibold text-gray-900 dark:text-white">
						Recent Activity
						<span
							class="ml-2 text-sm font-normal text-gray-500 dark:text-gray-400"
						>
							({activities.length} items)
						</span>
					</h3>
				</div>

				<div class="overflow-x-auto">
					<table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
						<thead
							class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400"
						>
							<tr>
								<th scope="col" class="p-4">Transaction ID</th>
								<th scope="col" class="p-4">Type</th>
								<th scope="col" class="p-4">Description</th>
								<th scope="col" class="p-4">Amount</th>
								<th scope="col" class="p-4">Date & Time</th>
								<th scope="col" class="p-4">Status</th>
							</tr>
						</thead>
						<tbody>
							{activities.map((activity) => (
								<tr class="border-b dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-700">
									<td class="p-4 font-medium text-gray-900 dark:text-white">
										{activity.id}
									</td>
									<td class="p-4 text-gray-900 dark:text-white">
										{activity.type}
									</td>
									<td class="p-4 text-gray-900 dark:text-white">
										{activity.description}
									</td>
									<td class="p-4 font-mono font-semibold text-gray-900 dark:text-white">
										{formatCurrency(activity.amount)}
									</td>
									<td class="p-4 text-gray-900 dark:text-white">
										{formatDateTime(activity.date, activity.time)}
									</td>
									<td class="p-4">
										<span
											class={`px-2.5 py-0.5 rounded-md text-xs font-medium ${getStatusColor(activity.status)}`}
										>
											{activity.status}
										</span>
									</td>
								</tr>
							))}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
</LayoutSidebar>
